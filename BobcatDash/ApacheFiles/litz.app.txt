<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    ServerName litz.app
    ServerAlias www.litz.app
    DocumentRoot /var/www/litz.app
    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
RewriteEngine on
RewriteCond %{SERVER_NAME} =www.litz.app [OR]
RewriteCond %{SERVER_NAME} =litz.app
RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
</VirtualHost>
